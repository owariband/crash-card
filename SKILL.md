---
name: crash-card
description: 将知识主题、学习材料或用户口述核对并整理为中文学习卡；生成 1200×1600 的讲解、自查、解答三类 PNG，按概念、公式、流程、对比、例题或纠错选择版式。用于费曼式讲解带练、快速复习和按语义核对口述。
metadata:
  short-description: 费曼式知识讲解、口述核对与三类 PNG 学习卡
---

# Crash Card

以“自己解释 → 找到缺口 → 补学 → 再解释”组织学习；以知识单元组织卡片。一个单元对应一种能解释、应用和检验的能力，可以跨多张卡。新版输出 **1200×1600 px、3:4 竖版 PNG**，统一使用 `$crash-card`。

优先级：**准确 → 清楚 → 简洁 → 美观**。简洁来自去掉重复和空话。保留必要前提、因果链、关键中间步骤与例子；段落长度、要点数和卡片数由内容决定。让每张卡完成一个有实质内容的子问题，不把完整解释切成没有依据的口号。

## 选择入口

- **制作卡片**：有主题即可核对资料、组织单元并制作。用户明确要求制作或已批准方案时直接完成，不另设草稿审批。用户只要方案或草稿时停在该阶段。
- **核对口述 / 讲解带练**：读取 [references/workflow.md](references/workflow.md)，利用已有自述，按语义判断、补讲、变式复检。不要求先制卡，也不强迫没有自述的用户先答题。
- **快速自查**：从题目开始，答后展示解答或核对反馈。一次留下一个主要待答任务；用户要完整材料时直接分组交付。

输入可以是当前对话语音、平台转写或粘贴文字。只评价实际可见的内容；本 Skill 不自行录音，也不承诺额外语音识别。没有实际回答时，学习状态保持“未检验”。

简短主题可以直接整理必要知识。输入对话按普通材料提取关注的知识点与疑问，无需逐轮还原；区分材料中的观点和用户明确作为自己理解提供的自述，只有后者能作为掌握情况的证据。

## 建立可核对的知识基线

先明确本次能力目标、范围和必要技术版本；识别前置知识、已提出问题与已暴露误解。按适用性覆盖定义、机制、条件、例子及边界，范围较大时拆成相连单元。首次出现的术语就地解释，条件紧邻结论，类比给出适用边界。

涉及版本、实现或不确定的关键事实时查证可靠来源；卡片中的关键结论要能对应来源。未核验的核心事实不能写成肯定式解答。若暂时无法核验，继续完成独立部分，具体标明阻塞目标及所缺依据；该目标仍未完成。仅用户同意缩小范围时才移出交付。

“讲透”以本次范围为界：必要机制能连起来、例子能走通、边界明确、显式问题与已暴露误解有处理记录。内容讲完与学习者掌握分开判断。

## 设计三类卡片

制卡前读 [references/design.md](references/design.md)；编写 JSON 时读 [references/card-schema.md](references/card-schema.md)。卡片用途与版式分别选择：

| 用途 | 职责 |
|---|---|
| `explanation` 知识讲解 | 解释一个完整子问题，提供结论及必要依据 |
| `self_check` 自查问题 | 一个主要考查目标，提供必要背景，要求生成自己的解释 |
| `answer` 解答 | 最短充分答案、关键理由、核对标准与补学定位，可连续多张 |

主体版式有概念、公式、流程、对比、例题、纠错六类；按知识结构选用，不要求每包集齐。深入模式展开推理与算例，需要时连续制卡，不压缩为更小的字。

题目采用重建概念、解释机制、追踪过程、改变条件或辨错等任务。提供的信息与考查目标匹配：考机制可以给公式，考公式回忆则不预先给出。标题、题干、图和提示都要检查是否泄漏目标答案。

每题的 `solution` 是参考解答与口述核对的共同来源。解答卡通过引用使用其正文和标准，文字稿从同一数据导出。每个自查问题都有独立解答 PNG；答案足以核对当前题，不能仅指向讲解卡。允许准确的等价说法，不以关键词命中或逐字一致判分。

## 渲染与交付

写入本地卡片前读 [references/output-storage.md](references/output-storage.md)，按持久配置或本次指定位置选择根目录，并为本批创建独立子目录；纯文本交流无需配置目录。将本次读取的 `SKILL.md` 所在目录的绝对路径设为 `SKILL_ROOT`，批次目录的绝对路径设为 `OUTPUT_DIR`。脚本和资源从 `SKILL_ROOT` 定位，产物写入 `OUTPUT_DIR`，不依赖终端当前目录。

执行前读 [references/runtime.md](references/runtime.md)。新版由本地 Python 主渲染器完成实际字体测量、布局和绘制；旧 manifest 通过 legacy 路径处理。流程为：

1. 写入 v2 manifest，核对单元覆盖、题答关系、来源和题面信息。
2. 运行环境预检、manifest 校验、PNG 渲染和输出校验；具体命令见 runtime。
3. 检查 `render-report.json`，并逐张查看 PNG 的内容完整性、公式、图表、裁切、重叠和答案泄漏；再以约 400 px 宽检查阅读效果。结构校验不能替代事实与视觉核对。
4. 溢出时删重复、改写、换布局或按完整子问题拆卡后重渲染。保留必要内容与可读字号；渲染失败不等于已交付。
5. 交付 `explanation/`、`self-check/`、`answer/` 三组 PNG，以及同源 `manifest.json`、`transcript.md`、`answer-key.md`、`oral-rubric.json`、`review.md`、`sources.md` 和渲染报告。先看自查组，答后核对解答组；manifest 与文字稿含明文答案。

PNG 独立可读；可选画廊属于辅助阅读器。渲染器导出复习入口与卡片映射，不根据出图结果生成学习评分。真实作答记录单独维护，详见 workflow。

## 完成判定

- **内容**：原始范围、前置和显式疑问有对应解释；关键事实已核验，结论的条件与推理完整。
- **题答**：每题都有同源解答与语义核对点；合理改述可通过，关键矛盾不能被其他答对项抵消。
- **图像**：三类均有 1200×1600 PNG；脚本校验与视觉检查通过，所有预期内容块实际绘制且可读。
- **学习**：只根据实际作答记录未检验、待修补、有提示完成、当场独立通过或延迟通过。仅制作完成时不声称已掌握。

用户暂停时给出已完成目标、当前缺口与恢复入口。需要解释或调整学习方法时再读 [references/learning-evidence.md](references/learning-evidence.md)；整套 Skill 没有 ADHD 专属效果验证，不把设计参数宣传为最优学习处方。
